---
name: pull-changes
description: >
    This describes how to pull changes from a cl
    when continuing to work on feedback provided.
---

When working on a cl that already exists, you can pull it
with the command `git cl patch -b change-<cl id> <cl id>`.

You should first check if this branch `change-<cl id>` exists
locally.
